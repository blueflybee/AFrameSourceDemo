<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="myorg.aframe.aframesourcedemo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:name=".AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ExStorageActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".IntentActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".utils.PermissionUtils$PermissionActivity"
            android:screenOrientation="portrait" />


        <activity
            android:name=".DataBindingActivity"
            android:screenOrientation="portrait" />


        <receiver
            android:name=".IntentActivity$BroadcastReceiver1"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1">
                <action android:name="com.test.myorder.action" />
            </intent-filter>
        </receiver>


        <receiver
            android:name=".IntentActivity$BroadcastReceiver2"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="500">
                <action android:name="com.test.myorder.action" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".IntentActivity$BroadcastReceiver3"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="com.test.myorder.action" />
            </intent-filter>
        </receiver>

    </application>

</manifest>